# Nocarz IUM project — Python 3.12
# Core data / ML stack (already present in the environment, pinned for reproducibility)
pandas>=2.3
numpy>=2.3
scikit-learn>=1.7
scipy>=1.16
matplotlib>=3.10
seaborn>=0.13
joblib>=1.3

# Microservice
fastapi>=0.123
uvicorn>=0.30        # ASGI server (the only package that must be pip-installed)
pydantic>=2.0

# Notebooks
jupyter>=1.0
pytest>=7.0
